# MYEONGRO 작업 정책

이 문서는 저장소 내 기존 계획서, 에이전트 실행 문서, 작업별 지시보다 우선한다.
사용자가 대화에서 새 지침을 주면 해당 지침이 이 문서보다 우선한다.

## 작업 수행

- 작은 기능과 단일 작업은 주 에이전트가 직접 구현한다.
- 하위 에이전트는 서로 독립적인 작업이 3개 이상일 때만 사용한다.
- 파일이나 상태를 공유해 순차 실행이 필요한 작업은 하위 에이전트 수를 늘리는
  근거로 보지 않는다.
- 구현 에이전트마다 별도의 명세 리뷰 에이전트나 코드 품질 리뷰 에이전트를
  자동으로 배정하지 않는다.
- 필요한 리뷰는 주 에이전트가 변경 범위와 위험도에 맞춰 직접 수행한다.

## 테스트

- 각 구현 단계에서는 변경과 직접 관련된 테스트만 실행한다.
- 전체 테스트, 타입 검사, 린트, 빌드 같은 종합 검증은 마일스톤 완료 시 마지막에
  한 번 실행한다.
- 고위험 변경이나 사용자의 명시적 요청이 있으면 더 넓은 검증을 추가할 수 있다.

## 리뷰 전담 스레드

- MYEONGRO의 리뷰 전담 스레드 이름은 `MYEONGRO Review Desk`다.
- 리뷰 전담 스레드 ID는 `019ebac7-a70b-7d72-a8d1-af39af21a3fb`다.
- 구현 스레드는 마일스톤 완료 또는 머지 후보 상태가 되면 반드시
  `MYEONGRO Review Desk`에 리뷰를 요청한다.
- 사용자가 명시적으로 생략을 허용한 경우를 제외하면
  `implement -> review -> fix/re-review or merge` 흐름을 따른다.
- 리뷰 요청 없이 바로 머지하거나, 리뷰 결과를 확인하지 않은 채 다음 마일스톤으로
  넘어가지 않는다.

## 브랜치와 커밋 흐름

- 작업은 기본적으로 한 목적만 담는 브랜치에서 진행한다.
- 브랜치 이름은 의도가 드러나게 유지한다.
  - 예: `feature/free-reading-records`
  - 예: `fix/reading-failure-state`
  - 예: `chore/flyway-baseline`
  - 예: `review/spring-migration-openai-generator`
- 하나의 브랜치에는 서로 다른 마일스톤의 큰 변경을 섞지 않는다.
- 리뷰 요청 전에는 적어도 한 번 이상 로컬 커밋을 남긴다.
- 리뷰 반영은 같은 브랜치에 추가 커밋으로 쌓는다.
- 사용자가 명시적으로 요청하지 않으면 `amend`나 히스토리 재작성에 의존하지
  않는다.

## 구현 스레드 완료 조건

- 관련 테스트가 통과한다.
- 마일스톤 종료 시 전체 검증 1회를 완료한다.
- 현재 브랜치의 목표, 변경 요약, 남은 리스크를 정리할 수 있다.
- 이 상태가 되면 구현 스레드는 리뷰 요청을 보낸다.

## 리뷰 자동 순환 규칙

- 구현 스레드는 리뷰 요청을 보낼 때 아래 템플릿을 그대로 사용한다.
- 리뷰 전담 스레드는 리뷰 결과를 아래 응답 템플릿으로 반환한다.
- 구현 스레드는 리뷰 요청에 `요청 스레드 ID`를 포함해, 리뷰 결과가 어느 스레드로
  돌아가야 하는지 명시한다.
- 리뷰 전담 스레드는 `요청 스레드 ID`가 있으면 해당 스레드에 결과를 직접
  회신한다.
- 리뷰 요청이 Codex delegation 형태로 들어오면, 본문에 적힌
  `source_thread_id`를 `요청 스레드 ID`와 동일하게 취급한다.
- 구현 스레드는 리뷰 결과를 받으면 상태를 `changes-requested` 또는
  `approved`로 해석하고 즉시 다음 행동을 결정한다.
- `changes-requested`이면 같은 브랜치에서 수정 커밋을 만들고 재리뷰 요청을
  보낸다.
- `approved`이면 머지 후보로 전환하되, 머지는 사용자 승인 또는 명시적 위임이
  있을 때만 진행한다.
- 리뷰 결과를 받은 구현 스레드는 finding을 해결하지 않은 채 새 기능 구현으로
  넘어가지 않는다.

상태 키:

- `review-requested`: 구현 스레드가 리뷰를 요청한 상태
- `changes-requested`: 리뷰 스레드가 수정 후 재리뷰를 요구한 상태
- `approved`: 리뷰 스레드가 블로킹 이슈 없음을 확인한 상태

## 리뷰 요청 규칙

- 구현 스레드는 완료 직후 `MYEONGRO Review Desk`에 아래 템플릿으로 메시지를
  보낸다.
- 리뷰 요청은 말로만 "끝났다"가 아니라, 리뷰에 필요한 실행 맥락을 포함해야 한다.
- 템플릿 첫 줄의 `리뷰 상태`는 항상 `review-requested`로 적는다.

```text
리뷰 요청:
- 리뷰 상태: review-requested
- 요청 스레드 ID:
- 대상 저장소:
- 브랜치/커밋:
- 작업 목표:
- 변경 요약:
- 실행한 검증:
- 특별히 봐야 할 점:
- 기획/정책과 달라진 판단:
```

- `브랜치/커밋`에는 현재 검토 대상 HEAD를 정확히 적는다.
- `요청 스레드 ID`에는 리뷰 결과를 다시 받아야 하는 구현 스레드의 thread id를
  적는다.
- `변경 요약`은 사용자 영향과 아키텍처 변경 중심으로 적는다.
- `실행한 검증`에는 실제 실행한 명령 또는 확인한 흐름만 적는다.
- `특별히 봐야 할 점`에는 불안한 지점, 트레이드오프, 의도적으로 남긴 제한을
  적는다.
- `기획/정책과 달라진 판단`이 없다면 `없음`이라고 명시한다.

## 리뷰 응답 규칙

- `MYEONGRO Review Desk`는 리뷰 결과를 아래 템플릿으로 회신한다.
- finding이 하나라도 있으면 `리뷰 상태: changes-requested`를 사용한다.
- 블로킹 이슈가 없으면 `리뷰 상태: approved`를 사용한다.
- finding은 심각도 순서로 적고, 각 항목은 파일 경로와 핵심 이유를 포함한다.
- 구현 스레드는 이 응답을 그대로 작업 기준으로 삼아 수정 범위를 확정한다.

```text
리뷰 결과:
- 리뷰 상태: changes-requested | approved
- 회신 대상 스레드 ID:
- 대상 저장소:
- 브랜치/커밋:
- findings:
  - [P1|P2|P3] 파일/지점: 문제 요약
- 열린 질문/가정:
- 최종 판단:
```

- `approved`일 때 `findings`는 `없음`이라고 적는다.
- `회신 대상 스레드 ID`는 리뷰 요청에 들어 있던 `요청 스레드 ID`와 동일해야
  한다.
- `최종 판단`은 `재리뷰 필요`, `머지 후보`, `추가 확인 필요` 중 하나로 적는다.

## 리뷰 결과 처리

- 리뷰 결과에 치명적 또는 중간 수준의 finding이 있으면 같은 브랜치에서 수정 후
  다시 리뷰 요청을 보낸다.
- 리뷰 결과가 `approved` 또는 사용자의 명시적 승인 상태가 되면 머지 후보로
  본다.
- 리뷰 결과를 무시하고 머지하지 않는다.
- 구현 스레드는 재리뷰 요청 시 변경된 커밋과 해결한 finding을 함께 적는다.
- 재리뷰 요청에는 직전 리뷰 결과를 기준으로 어떤 항목을 닫았는지 명시한다.

재리뷰 요청 템플릿:

```text
재리뷰 요청:
- 리뷰 상태: review-requested
- 요청 스레드 ID:
- 대상 저장소:
- 브랜치/새 커밋:
- 반영한 finding:
- 추가 변경 요약:
- 다시 실행한 검증:
- 아직 남아 있는 우려:
```

- `반영한 finding`은 이전 리뷰의 항목 번호나 요약 문구와 연결되게 적는다.
- `아직 남아 있는 우려`가 없다면 `없음`이라고 적는다.

## 머지 규칙

- 머지는 사용자 승인 또는 명시적 위임이 있을 때만 진행한다.
- 머지 전 기준:
  - 최신 리뷰 결과 확인
  - 필요한 검증 완료
  - 남은 known risk를 사용자 또는 리뷰 스레드에 공유
- 급한 예외 대응이 아니라면 리뷰 없이 `main`, `dev`, 장기 통합 브랜치로 바로
  넣지 않는다.

## Notion 보고

- Notion 보고는 개별 작업이나 하위 에이전트 단위로 만들지 않는다.
- 하나의 마일스톤이 끝날 때 해당 마일스톤 전체를 요약한 페이지 1개를 작성한다.
- 페이지에는 목표, 주요 변경, 검증 결과, 미해결 사항과 다음 마일스톤을 기록한다.

database:
  url:
  username:
  password:

openai:
  api-key:
  model: gpt-5.6-luna

oauth:
  kakao:
    client-id:
    client-secret:
    redirect-uri: http://localhost:8080/login/oauth2/code/kakao
  google:
    client-id:
    client-secret:
    redirect-uri: http://localhost:8080/login/oauth2/code/google

frontend:
  origin: http://localhost:3000

# 운영 Redis에 password가 필요한 경우에만 실제 secret 파일에 추가
# spring:
#   data:
#     redis:
#       password: change-me
